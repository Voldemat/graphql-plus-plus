query GetUserNotificationSettings($id: String!) {
    getUserNotificationSettings(id: $id) {
        __typename
        ... on BaseError {
            status
        }
        ... on UserNotificationSettings {
            ...UserNotificationSettings
        }
    }
}