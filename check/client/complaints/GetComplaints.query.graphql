query GetComplaints($skip: Int! = 0, $limit: Int! = 20) {
  getComplaints(skip: $skip, limit: $limit) {
    __typename
    ... on BaseError {
        status
    }
    ... on ComplaintsList {
        ...ComplaintsList
    }
  }
}
