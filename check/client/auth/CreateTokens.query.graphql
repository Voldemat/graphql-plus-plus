query CreateTokens ($login: String!, $password: String!, $mustBeAdmin: Boolean! = false){
  createTokens(login: $login, password: $password, mustBeAdmin: $mustBeAdmin){
       __typename
    ... on ErrorWithFields {
      status
      fields
    }
    ... on TokenPair {
        accessToken
        refreshToken
    }
  }
  }
