query GetStatistics($params: StatisticsIn!) {
  getStatistics(params: $params) {
    __typename
    ... on ErrorWithFields {
      status
      fields
    }

    ... on Statistics {
      ...FullStatistics
    }
  }
}
