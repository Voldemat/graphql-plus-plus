mutation ResetPassword($target:String!, $newPassword:String!, $transport:CodeTransport!){
    resetPassword(target: $target, newPassword: $newPassword, transport: $transport){
        status,
        fields
    }
}