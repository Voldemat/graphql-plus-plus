query getRecommendedUsers($sessionId: String!, $limit: Int){
    getRecommendedUsers(sessionId: $sessionId, limit: $limit){
        __typename
        ... on BaseError {
            status
        }
        ... on UserList {
            ...UserList
        }
    }
}