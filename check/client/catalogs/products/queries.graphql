query RetrieveProduct ($id: String!) {
    retrieveProduct(id: $id) {
        __typename
        ... on BaseError {
            status
        }
        ... on Product {
            ...Product
        }
    }
}