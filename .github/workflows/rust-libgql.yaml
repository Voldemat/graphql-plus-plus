name: "rust-libgql"

on:
  push:
    paths:
    - rust/libgql/**
    - .github/workflows/rust-libgql.yaml

jobs:
  build:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        BUILD_TARGET: [release]
    outputs:
      release_built: ${{ steps.set-output.outputs.release_built }}
    permissions:
      id-token: write
      contents: read
    steps:
    - uses: actions/checkout@v5
    - name: Build binaries in "${{ matrix.BUILD_TARGET }}" mode
      working-directory: rust/libgql
      run: cargo build --profile ${{ matrix.BUILD_TARGET }}
    - name: Run tests in "${{ matrix.BUILD_TARGET }}" mode
      working-directory: rust/libgql
      run: cargo test --profile ${{ matrix.BUILD_TARGET }} --features=formatter
    - name: Get crates.io token
      if: github.ref_type == 'tag'
      uses: rust-lang/crates-io-auth-action@v1
      id: auth
    - name: Publish crate
      if: github.ref_type == 'tag'
      working-directory: rust/libgql
      env:
        CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}
      run: |
        sed -i.bak 's/^version = "0.0.1"/version = "${{ github.ref_name }}"/' Cargo.toml
        rm Cargo.toml.bak
        cargo check
        cargo publish --allow-dirty
